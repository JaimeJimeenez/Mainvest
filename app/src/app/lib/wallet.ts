import { IWallet } from "../interface/financial/iWallet";

export class Wallet {

  static getWalletsInfo(data : any[]) : any[] {
    const walletsInfo : any[] = [];

    data.forEach(() => {
      
    });

    return walletsInfo;
  }

  static parseWallets() : IWallet[] {
    return [];
  }
}
